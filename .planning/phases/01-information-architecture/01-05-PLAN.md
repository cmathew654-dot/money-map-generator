---
phase: 01-information-architecture
plan: 05
type: execute
wave: 4
depends_on: [01-04]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [IA-01, IA-02, IA-03, IA-04, IA-05, IA-06]

estimate:
  tokens: 34000
  raw_tokens: 17000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every e2e or unit failure standing at the end of Phase 1 is either pre-existing and named, or a recorded deliberate ruling — none is silent."
    - "Cyril has opened the panel and confirmed he can name the section for every kind of input without checking the code."
  artifacts:
    - "A pass/fail disposition table in the SUMMARY covering the full unit suite and the canonical e2e project"
    - ".planning/ROADMAP.md and .planning/REQUIREMENTS.md — IA-01 through IA-06 marked complete"
  key_links:
    - "6 parked-red e2e specs (3 accessibility, 2 multitab, 1 interaction regression) -> Phase 4 VERIFY-02, not this phase"
---

<objective>
Prove Phase 1 landed, and put the panel in front of the person whose judgment defines the goal.

Purpose: Phase 1's success criteria are stated from Cyril's point of view — "can look at any field and know exactly which section it lives in." No automated check can assert that. The gate is therefore two-part: the suites prove nothing regressed, and Cyril proves the goal was met.

Output: A disposition table separating pre-existing red from anything this phase introduced, and Cyril's confirmation or his specific objections.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/TESTING.md
@.planning/phases/01-information-architecture/01-01-SUMMARY.md
@.planning/phases/01-information-architecture/01-02-SUMMARY.md
@.planning/phases/01-information-architecture/01-03-SUMMARY.md
@.planning/phases/01-information-architecture/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite gate with explicit dispositions</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <precondition>Plans 01-01 through 01-04 are committed on `feat/left-panel-redesign`; `git status` is clean before the gate runs, so a failure is attributable to committed code and not to a dirty tree.</precondition>
  <action>
    Run the full unit suite and the canonical e2e project. Scope, deliberately: the canonical project only (`chromium-1280x720` per PROJECT.md), not all 15 Playwright projects. The full cross-project sweep is VERIFY-01, Phase 4's requirement — running it here would duplicate Phase 4 and add roughly an order of magnitude of wall clock for information this phase does not act on.

    Expected duration: unit suite well under two minutes; canonical e2e project several minutes at `--workers=1`. If either run exceeds three times that expectation, stop and report state rather than waiting or silently retrying.

    Classify every failure into exactly one of three buckets and put the table in the SUMMARY:
    - **Pre-existing parked red** — one of the 6 known specs (3 accessibility, 2 multitab, 1 interaction regression). Name the spec. Do not fix. Do not unpark. Their disposition is VERIFY-02, Phase 4.
    - **Deliberate realignment** — a test whose expectation this phase intentionally changed. Cite the requirement ID (IA-02 for the fine-print move, IA-05 or IA-06 for a rename) and the plan that changed it. These should already be green, having been updated in the plan that caused them; anything still red here is a miss, not a ruling.
    - **New breakage** — anything else. This blocks the phase. Fix it or halt for a ruling; do not carry it forward and do not annotate it away.

    Only after the table is complete: mark IA-01 through IA-06 complete in `.planning/REQUIREMENTS.md` and update the Phase 1 row in `.planning/ROADMAP.md` Progress table. Do not mark anything complete on the strength of a plan having run — mark it on the strength of the check.
  </action>
  <verify>
    <automated>npm run test && npx playwright test --project=chromium-1280x720</automated>
  </verify>
  <done>Every failing test at the end of Phase 1 sits in a named bucket with a stated reason; the "new breakage" bucket is empty; IA-01 through IA-06 are marked complete against evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 1 changed the Data panel's information architecture and copy — no visual redesign, which is Phase 3:
    - Three fields that printed on the exported map but had no control anywhere now have one: a value tag on each account card, a need tag in Need, a qualifier on each income source row (IA-04).
    - Fine print's editor moved from Need into Income, along with its filter index (IA-02).
    - Positions and Sub-accounts each gained a help line explaining the difference (IA-03).
    - Notes gained an empty state; an all-sections-filtered-out query now says so instead of showing a blank panel.
    - The after-tax naming collision was resolved per the ruling given in plan 04 (IA-05), and the as-needed withdrawal field's panel label now reads as the phrase the map prints (IA-06).
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the app, then the Data panel.

    1. **The goal question.** Walk the five sections. For each kind of input — income source, account, position, sub-account, need line, note, fine print line — say out loud which section it lives in without checking the code. Any hesitation is the finding; name the field.
    2. **IA-04.** Expand an account: there is a value tag field under Value. Type a value in it, in the need tag field, and in an income row's qualifier field. Export a PDF or PNG. All three appear on the artifact.
    3. **IA-04, the sharp case.** Type your need tag text into the filter box. The Need section matches and the field holding it is visible and editable — it is no longer searchable-but-unreachable.
    4. **IA-02.** Fine print's editor is in Income. Nav is still five buttons. Filter for a fine print label: Income matches, Need does not.
    5. **IA-03.** Read the two help lines under Positions and Sub-accounts. If the distinction still is not obvious, that is the finding — the wording is yours to revise.
    6. **Empty states.** Delete every note: Notes says it is empty. Type gibberish into the filter: the panel says nothing matches rather than going blank.
    7. **IA-05 / IA-06.** The after-tax ruling you gave reads correctly in the panel and on the map. The withdrawal field's label matches the chip the map prints.

    Ignore field styling, spacing, and density throughout — those are deliberately untouched and are Phase 3's scope.
  </how-to-verify>
  <resume-signal>Type "approved", or name the specific fields or strings that are still wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-|-|
| test results -> completion claim | The only boundary this plan crosses: an unproven claim of completion propagates into Phase 2's assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-|-|-|-|-|-|
| T-01-12 | Repudiation | Marking IA-01..IA-06 complete | medium | mitigate | Task 1 forbids marking complete before the disposition table exists, and requires the "new breakage" bucket to be empty. The pre-existing parked-red set is enumerated by name so it cannot absorb a genuine regression. |
| T-01-13 | Spoofing | Pre-existing red laundered as "deliberate realignment" | medium | mitigate | The three buckets are mutually exclusive and each entry names either a known parked spec or a requirement ID and the plan that changed it. A realignment that is still red is defined as a miss, not a ruling. |
| T-01-14 | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed by this plan. |
</threat_model>

<verification>
- `npm run test` result recorded.
- `npx playwright test --project=chromium-1280x720` result recorded, headless.
- Disposition table present in the SUMMARY with an empty "new breakage" bucket.
- Cyril's checkpoint response recorded verbatim.
</verification>

<success_criteria>
All six Roadmap Phase 1 success criteria hold, and criterion 2 — the one no test can assert — is confirmed by the person it describes.
</success_criteria>

<output>
Create `.planning/phases/01-information-architecture/01-05-SUMMARY.md` when done.

Commit, conventional, on `feat/left-panel-redesign` only:
- `docs(01): record phase 1 verification dispositions`

Any fix Cyril's checkpoint requests gets its own commit with its own conventional subject; do not fold panel changes into the verification commit.
</output>
