---
phase: 01-information-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/superpowers/specs/2026-08-02-canvas-first-editor-design.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [IA-01]

estimate:
  tokens: 26000
  raw_tokens: 13000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A developer opening the stale canvas-first editor spec is told, in its first screen, that its Data panel description is superseded and where the current one lives."
    - "The documented section taxonomy is proven against the shipped code, not asserted."
    - "PROJECT.md no longer carries 'confirmed at Phase 1 kickoff' as a pending decision."
  artifacts:
    - "docs/superpowers/specs/2026-08-02-canvas-first-editor-design.md — superseded banner at the top"
    - ".planning/PROJECT.md — Key Decisions row resolved"
    - ".planning/REQUIREMENTS.md — traceability table covering all 19 requirement IDs"
  key_links:
    - "stale spec banner -> .planning/phases/01-information-architecture/01-UI-SPEC.md Section Taxonomy table -> src/form/Form.tsx data-form-section attributes"
---

<objective>
Make the shipped s51 Ledger accordion the documented ground truth, and prove that documentation against the code rather than asserting it.

Purpose: `docs/superpowers/specs/2026-08-02-canvas-first-editor-design.md` describes the Data panel as a plain sectioned form. The panel actually shipped in s51 as an accordion — a different interaction model — and that rebuild lives only in handoff prose. Anyone reading the spec today is misled. PROJECT.md carries this as an unresolved Key Decision.

Output: A superseded banner on the stale spec, a code-proven taxonomy, and two resolved planning-doc rows.

Runs in parallel with plan 01 (wave 1): this plan touches only documentation, zero overlap with `src/form/Form.tsx`.

Ruling recorded: this plan carries no `tracer` task. Plan 01 leads the phase with the tracer slice; there is no architecture to prove end-to-end in a documentation change, and adding a nominal tracer here would be ceremony with no information return.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INGEST-CONFLICTS.md
@.planning/phases/01-information-architecture/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove the documented taxonomy against shipped code</name>
  <files>.planning/phases/01-information-architecture/01-UI-SPEC.md</files>
  <action>
    Confirm the UI-SPEC Section Taxonomy table (five rows: client, income, accounts, need, notes) matches the five `data-form-section` attributes actually rendered by `src/form/Form.tsx`, and that the section-nav label map at `Form.tsx:1459-1465` carries the same five keys.

    Verified during planning on 2026-08-08: `data-form-section` values are `income` (554), `accounts` (1148 region), `need` (520), `notes` (1262), `client` (ClientSection at 1308+); `sectionLabels` at 1459-1465 lists exactly Client / Income / Accounts / Need / Notes. Re-run the check rather than trusting this line — the point of the task is the evidence, not the claim.

    If the check finds a mismatch, do NOT silently edit the UI-SPEC: the UI-SPEC is APPROVED. Record the mismatch in the SUMMARY and halt for a ruling.

    If the check passes, append one line to the UI-SPEC under the Section Taxonomy table recording the verification and its date.
  </action>
  <verify>
    <automated>grep -o 'data-form-section="[a-z]*"' src/form/Form.tsx | sort -u | wc -l | grep -qx '5' && grep -c "data-form-section=" src/form/Form.tsx</automated>
  </verify>
  <done>Exactly five distinct section identifiers exist in the shipped component and they match the five documented in the UI-SPEC taxonomy table; the verification line is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Mark the stale editor spec superseded</name>
  <files>docs/superpowers/specs/2026-08-02-canvas-first-editor-design.md</files>
  <action>
    Insert a banner immediately after the document's title heading — before any other content, so it is impossible to read the stale description without seeing it first.

    Banner content, stated plainly, no hedging: the document's description of the Data panel as a plain sectioned form is superseded as of 2026-08-08; the panel shipped in s51 as a Ledger accordion; the current ground truth is the Section Taxonomy table in `.planning/phases/01-information-architecture/01-UI-SPEC.md`. State that the rest of the document (canvas-first shell, 72px rail, on-demand panels, the "inspector stays quick-actions only" constraint) remains in effect — only the Data panel description is superseded.

    Do not delete or rewrite the stale section. Superseding is the record; deleting it destroys the trail of why the ingest conflict existed.
  </action>
  <verify>
    <automated>head -20 docs/superpowers/specs/2026-08-02-canvas-first-editor-design.md | grep -qi 'supersed' && echo BANNER_PRESENT</automated>
  </verify>
  <done>The stale spec's first screen states which part of it is superseded and points at the current source of truth.</done>
</task>

<task type="auto">
  <name>Task 3: Close the two open planning-doc rows</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <action>
    In `.planning/PROJECT.md` Key Decisions table, change the outcome of the row "Treat s51's shipped Ledger accordion Data panel as ground truth over canvas-first-editor-design.md's stale description" from "Pending — confirmed at Phase 1 kickoff" to confirmed, dated 2026-08-08, citing the task-1 code check and the task-2 banner as the evidence.

    In `.planning/REQUIREMENTS.md`, repair the Traceability table. It currently lists 13 rows and claims "v1 requirements: 13 total / Unmapped: 0", but six requirement IDs defined in the body are absent from the table: IA-04, IA-05, IA-06 (Phase 1), FLOW-04, FLOW-05 (Phase 2), CRAFT-05 (Phase 3). Add the six missing rows in ID order and correct the coverage counts to 19.

    This defect was found during Phase 1 planning; fix it here because this plan is the only documentation-only plan in the phase and the arithmetic is currently wrong in a file other phases will read.
  </action>
  <verify>
    <automated>grep -cE '^\| (IA|FLOW|CRAFT|VERIFY)-[0-9]+ \|' .planning/REQUIREMENTS.md | grep -qx '19' && grep -q 'v1 requirements: 19 total' .planning/REQUIREMENTS.md && echo TRACEABILITY_OK</automated>
  </verify>
  <done>PROJECT.md's ground-truth decision is resolved with cited evidence; REQUIREMENTS.md traceability covers all 19 IDs and its counts agree with its own table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-|-|
| none crossed | This plan modifies three markdown files. No code path, no input handling, no build output, no dependency change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-|-|-|-|-|-|
| T-01-03 | Repudiation | Superseding a design document | low | mitigate | Banner supersedes rather than deletes; the stale text stays readable so the decision trail survives. Task 2 forbids removal explicitly. |
| T-01-04 | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed by this plan. |
</threat_model>

<verification>
- All three task `<automated>` checks pass.
- `git diff --stat` shows changes confined to the three files in `files_modified` — no source file touched by this plan.
</verification>

<success_criteria>
Roadmap Phase 1 criterion 1 is TRUE: the Data panel's documented structure matches what's actually in the code, and the document that said otherwise says so itself.
</success_criteria>

<output>
Create `.planning/phases/01-information-architecture/01-02-SUMMARY.md` when done.

Commits, conventional, on `feat/left-panel-redesign` only:
- `docs(01): verify shipped data panel taxonomy against code`
- `docs(01): mark stale editor panel description superseded`
- `docs(01): close ground-truth decision and repair requirement traceability`

The commit-msg hook blocks any message containing the superseded spec's directory path or a session prefix. Reference the file by name in the body only if needed — the paths above are deliberately absent from these subjects.
</output>
