---
phase: 01-information-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/form/Form.tsx
  - tests/form.test.ts
  - tests/export.test.ts
autonomous: true
requirements: [IA-04]

estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Cyril can type a value tag on an account card and see it print beside that account's value on the exported map."
    - "Cyril can type a need tag in the Need section; the filter no longer surfaces a field he cannot then edit."
    - "Cyril can type a qualifier on an income source row and see it print beside that amount on export."
    - "No string emitted by src/export/export.ts originates from a field with zero controls in the Data panel."
  artifacts:
    - "src/form/Form.tsx — three new controls: valueTag (AccountCard), needTag (NeedSection), qualifier (IncomeSection row)"
    - "tests/form.test.ts — render assertions for all three controls"
    - "tests/export.test.ts — assertion that a set tag/qualifier reaches the exported description"
  key_links:
    - "control onChange -> FormProps.onChange -> App.commitSnapshot -> book state -> src/export/export.ts:37,41,48"
    - "IncomeSource.qualifier -> src/model/vocab.ts:111 (editable qualifiers now feed the autocomplete vocabulary)"
---

<objective>
Give the three map-printed, UI-invisible fields real controls: `Account.valueTag`, `MoneyMapData.needTag`, `IncomeSource.qualifier`.

Purpose: Text prints on the client-facing exported map that the advisor cannot change anywhere except by hand-editing the JSON book file. `needTag` is the sharpest case — the Data panel filter indexes it (`Form.tsx:1478`) so the filter surfaces a field the user then cannot find. This is the highest-value item in Phase 1: it converts an invisible constraint into a visible control.

Output: Three controls in their owning sections, each wired through the existing `onChange` -> `commitSnapshot` path, each covered by a test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-information-architecture/01-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

@src/form/Form.tsx
@src/model/types.ts
@src/export/export.ts
</context>

<interface_context>
Verified 2026-08-08 by direct read — do not re-derive:

- `Account.valueTag?: string` — `src/model/types.ts:39`, comment "short qualifier rendered beside the value, e.g. 'est.'". Prints via `src/export/export.ts:48` as `money(account.value) + ' (' + account.valueTag + ')'`.
- `MoneyMapData.needTag?: string` — `src/model/types.ts:203`, "short qualifier rendered beside the monthly need". Prints via `src/export/export.ts:41`.
- `IncomeSource.qualifier?: string` — `src/model/types.ts:56`, 'e.g. "Gross", "After-Tax" — rendered after the amount'. Prints via `src/export/export.ts:37`. Sample data ships `qualifier: 'Gross'` (`src/model/samples.ts:13,101,108,115,200`), so it renders in practice today.
- `src/model/vocab.ts:111` already reads `source.qualifier` into the autocomplete vocabulary — making it editable feeds that index with no extra wiring.
- `src/model/book.ts:748,760,786` already validate all three as optional strings — no model or validation change is needed in this plan.

Existing control primitives in `Form.tsx` (reuse, do not invent): `TextField({ label, value, onChange, inputRef?, autocomplete? })`, `MoneyField({ label, value, onChange, inputRef? })`, `<p className="help-text">` for one-line guidance.

Insertion sites:
- `AccountCard` fields block — `Form.tsx:899-941` (`<div className="account-fields">`), Value `MoneyField` at 909-913.
- `NeedSection` — `Form.tsx:493-515`, `<div className="value-tag-fields need-fields">` (the wrapper is already named for the missing tag field).
- `IncomeSection` row — `Form.tsx:585-611` (`<div className="income-row-fields">`, Amount + Period).

Copy for the three new labels is new to this phase. Defaults specified per task; Cyril may revise wording later, the controls' existence is what is locked.
</interface_context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end value tag — account card to exported map</name>
  <files>src/form/Form.tsx, tests/form.test.ts, tests/export.test.ts</files>
  <behavior>
    - Rendering `AccountsSection` with an expanded account renders a text control labelled "Value tag" bound to `account.valueTag`.
    - Editing that control produces a new account object with the typed `valueTag`; every other account field is unchanged.
    - `describeMap`-style export output for an account carrying a value tag contains the tag in parentheses after the money value (assert against the existing `src/export/export.ts:48` format, do not change that format).
  </behavior>
  <action>
    Wire ONE field through every layer this phase touches, and no others yet.

    In `Form.tsx` `AccountCard`, insert a `TextField` immediately after the Value `MoneyField` (currently `Form.tsx:909-913`), inside `<div className="account-fields">`:
    label "Value tag", value `account.valueTag ?? ''`, onChange spreads the account with the new `valueTag`. Follow the exact prop shape of the sibling "Supporting note" `TextField` at 932-940. No `autocomplete` prop.

    Add one `<p className="help-text">` line directly under it: "Short qualifier printed beside the value on the map — e.g. est."

    Do not add CSS. Do not touch `src/model/types.ts`, `src/model/book.ts`, or `src/export/export.ts` — the type, the validator, and the print path all already exist and are verified. This task proves the seam, it does not widen it.

    Tests: add a render assertion to `tests/form.test.ts` following the existing `renderToStaticMarkup(createElement(...))` pattern used by the `need fine print` describe block at 273-291. Add one assertion to `tests/export.test.ts` alongside the existing `'After-tax income: $5,900.'` check at line 108 proving a set `valueTag` reaches the exported description.
  </action>
  <verify>
    <automated>npm run test -- tests/form.test.ts tests/export.test.ts</automated>
  </verify>
  <done>A value typed into the account card's new control appears in the exported map description; both suites pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Need tag control — close the searchable-but-uneditable gap</name>
  <files>src/form/Form.tsx, tests/form.test.ts</files>
  <behavior>
    - Rendering `NeedSection` renders a text control bound to `data.needTag`.
    - Filtering the Data panel for a string present only in `needTag` matches the Need section AND that section now contains an editable control holding that string.
  </behavior>
  <action>
    In `Form.tsx` `NeedSection`, add a `TextField` inside `<div className="value-tag-fields need-fields">` (opens at 494) directly after the "Monthly amount needed" `MoneyField` and its help line: label "Need tag", value `data.needTag ?? ''`, onChange `(needTag) => onChange({ ...data, needTag })`.

    Add one `<p className="help-text">` line under it: "Short qualifier printed beside the monthly need — e.g. net."

    Leave `sectionMatches` (`Form.tsx:1467-1487`) alone — `needTag` is already in the `need` blob at 1478 and stays there. This task makes that existing index entry honest rather than moving it.

    Note in the SUMMARY that the wrapper class `value-tag-fields` (present since before this phase) now has a tag field matching its name.

    Test: extend `tests/form.test.ts` with a `NeedSection` render assertion in the same static-markup style as the existing need-section tests.
  </action>
  <verify>
    <automated>npm run test -- tests/form.test.ts</automated>
  </verify>
  <done>Need section renders an editable control bound to `data.needTag`; the field the filter indexes is now reachable in the UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Income source qualifier control</name>
  <files>src/form/Form.tsx, tests/form.test.ts, tests/export.test.ts</files>
  <behavior>
    - Each income source row renders a text control bound to that source's `qualifier`.
    - Editing it updates only that row's `qualifier` (index-scoped, via the existing `updateSource(index, ...)` helper).
    - Export output for a source carrying a qualifier contains it in parentheses after the amount.
  </behavior>
  <action>
    In `Form.tsx` `IncomeSection`, add a `TextField` inside `<div className="income-row-fields">` (opens at 585) after the Period `<select>` block that closes at 610: label "Qualifier", value `source.qualifier ?? ''`, onChange `(qualifier) => updateSource(index, { ...source, qualifier })`.

    Add one `<p className="help-text">` line beneath the row's field group: "Printed beside the amount on the map — e.g. Gross."

    Pass the `autocomplete` prop with `{ bookTerms: vocabulary, seeds: noSeeds }`, matching the "Supporting note" pattern at `Form.tsx:932-936`. `src/model/vocab.ts:111` already harvests `source.qualifier` into `bookTerms`, so previously-typed qualifiers become suggestions with no additional wiring.

    Do not restyle the row or change its grid — field layout and density are CRAFT-01/CRAFT-02, Phase 3.

    Tests: `IncomeSection` render assertion in `tests/form.test.ts`; one export assertion in `tests/export.test.ts` proving a qualifier reaches the description via `src/export/export.ts:37`.
  </action>
  <verify>
    <automated>npm run test -- tests/form.test.ts tests/export.test.ts</automated>
  </verify>
  <done>All three formerly-invisible printed fields have controls. IA-04 is satisfied: nothing `src/export/export.ts` emits comes from a field with no UI control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-|-|
| user keyboard -> React controlled input -> book state -> localStorage | The only untrusted input path. No network, no server, no third party — the app is fully client-side by project constraint. |
| book state -> SVG/PDF/PNG export | Free text authored by the user is composed into an exported artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-|-|-|-|-|-|
| T-01-01 | Tampering | Three new free-text fields rendered into exported SVG/PDF via `src/export/export.ts` | low | accept | React escapes interpolated text and the export path is unchanged by this plan. Author and audience are the same advisor; no cross-user content. Accepting matches the treatment of the six existing free-text fields (`label`, `caption`, note text) already on this exact path. |
| T-01-02 | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no packages. No `package.json` dependency change is authorized; the milestone constraint is explicitly "no new runtime dependency." If an executor finds itself needing an install, halt and escalate rather than proceed. |
</threat_model>

<verification>
- `npm run test -- tests/form.test.ts tests/export.test.ts` passes.
- `npm run build` passes (TypeScript strict, `noUnusedLocals`/`noUnusedParameters` are on).
- No new file created outside the three listed in `files_modified`.
</verification>

<success_criteria>
Roadmap Phase 1 criterion 4 is TRUE: nothing prints on the exported map that cannot be edited in the UI. `valueTag`, `needTag`, and `qualifier` have controls.
</success_criteria>

<output>
Create `.planning/phases/01-information-architecture/01-01-SUMMARY.md` when done.

Commit per task, conventional commits, on `feat/left-panel-redesign` only:
- `feat(form): add account value tag control`
- `feat(form): add need tag control`
- `feat(form): add income source qualifier control`

Commit messages must not contain a session prefix or a docs path — a commit-msg hook hard-blocks those.
</output>
