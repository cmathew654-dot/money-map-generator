# Requirements

No PRD documents were present in this ingest set (classifications: 5 SPEC, 24 DOC, 0 ADR, 0 PRD). No requirements to extract under the PRD taxonomy.

The SPEC documents contain acceptance-criteria sections that read like requirements but are recorded under `constraints.md` per the SPEC extraction rule (type: nfr), not duplicated here, to avoid inventing a REQ-* namespace the source docs don't define.
