# Session 45 Handoff — spec-modernization merged; pan-hint fixed; baselines committed at cb5cf31; final gate remains (2026-08-03)

Written by Fable 5 at handoff point, updated after baseline regen. Worktree `C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40`, branch `repair/session-42` at `cb5cf31`, tree CLEAN. A full-suite e2e gate run was IN FLIGHT when this was written — background runs do not survive session end; if no results exist, rerun it (step 1). NEVER push, never add a remote — VC policy HARD-ENFORCED (repo hooks + settings deny + guardrails vc); upload ceremony goes through Cyril explicitly. One window owns the tree.

## What landed this session
- `5920923` merge of `lane/specmod` (`68ba15a`) — 3 OLD-DESIGN lease specs rewritten to takeover design + pan-hint assertion split. Fable-audited the full diff (frozen scope held: 3 test files only), reran tsc + vitest 537/537 in the lane, then merged. Targeted e2e on merged tip: 18 passed / 5 failed / 1 skipped — 4 failures are named inherited debt (certification:31 money-input rot, :46 wizard hardcode, :86 rapid-handoff race, multitab:84 money-input rot), 1 was a real today-gap (next bullet). New test names: app-guidance-s40 'the read-only banner offers an instant click-to-take-over of a live writer lease'; certification 'writer ownership follows the focused page, with the banner marking the read-only tab'; multitab-history 'a stale lease from a crashed tab is reclaimed promptly on load, no TTL wait'.
- `f8adfc7` **TODAY-GAP FOUND + FIXED**: `chrome-layout.spec.ts:96` (slice 12's own "honest pan hint" test) had failed on every tip since daa1b9a — `changeZoom` (toolbar zoom) called `dismissPanZoomHint()` before the zoomed state could render its pan wording, making the "states it once zoomed past fit" branch unreachable. Slipped through because lanes gated vitest+tsc only, the full-suite run predates the slice-11/12 merge, and the 06f733f baseline split predates the test's existence. Fix: one-line removal of `dismissPanZoomHint()` in `changeZoom` (App.tsx ~1364, de-minimis inline). Gesture dismissals (ctrl+wheel, pan-drag, Got it) untouched. Evidence: RED proven, then GREEN 6/6 targeted (chrome-layout + app-guidance-s40, chromium-1280x720) incl. the 640x360 coexistence test; tsc clean; vitest 537/537; 390px visual-reviewer check clean (hint persists post-zoom, two-line wrap, no overflow, 8px clear of Tidy map toolbar, no occlusion — screenshots were in session scratchpad, gone after session end; e2e coverage stands).
- Process rule added (Cyril-confirmed): heavy reference skills (claude-api ~180k tokens/load) NEVER load in main context — dispatch a sonnet low subagent that loads the skill and returns <10 lines. Now in global CLAUDE.md + project memory (`llm-api-lookups-via-sonnet-subagent`).

## Remaining sequence
0. DONE — visual baseline regen committed at `cb5cf31` (37 passed, 16 PNGs: 11 refreshed + 5 previously-untracked). Fable audited old-vs-new shots before committing. Two findings: (a) REAL, for dogfood verdict — slice 10's inspector fix works (full 'Duplicate'/'Cylinder'/'Short-term'/'Choose...' vs old truncated 'Duplica'/'S'/'Cho') but the RESET ITEM group now wraps to a second row the panel CLIPS at 1440x900 — 'Reset item' button hidden; no test guards it; Cyril decides accept vs follow-up CSS. (b) NOT a bug — wizard text-zoom baseline shows the toolbar mid-page over the sidebar: fullPage-screenshot stitching artifact of slice-11's position:fixed chrome; real viewport behavior is covered by the passing chrome-layout hit-test.
1. Final full-suite gate on the tip (Fable runs): `npx playwright test tests/e2e/` (~1.2h, 18 projects) + `npx vitest run` (537) + `npx tsc --noEmit`. Target: 0 UNEXPLAINED failures. Expected explained failures = inherited debt only: money-input locator rot (certification:31, multitab:84 et al.), wizard hasWarnings hardcode (certification:46 / App.tsx:1827), rapid-handoff races (certification:86, multitab:79-family), app-resilience:6/:77/:109, extended-certification:544/:707, adversarial-remediation:66, reflow:20, 200-client stress, WCAG items — per session-44 handoff §Legacy. chrome-layout:96 must now PASS; visual:44/:62 must pass post-regen. Anything else = investigate before proceeding.
3. Restart dev server for dogfood: kill the stale hot-reloaded vite on 4361 (was node pid 90452 — re-probe, don't trust the pid), restart from s40, confirm serving. Cyril dogfoods 4 routes: (1) drag object body ×3 — moves, stays, re-drags; (2) press-drag from text — moves, zero highlight; (3) dblclick text — editor opens, inner selection works; (4) second tab, drag there, return — banner appears then auto-recovers on return (or click banner). BONUS route: toolbar Zoom in with fresh hint → hint persists and gains pan wording. PLUS the two regen findings: (6) select an account at 1440x900 — is the clipped second inspector row (RESET ITEM) acceptable/reachable?; (7) at ≤700px width confirm the toolbar sits at the viewport bottom (mid-page look in the wizard baseline is a stitch artifact, not real).
4. UPLOAD ceremony (Cyril says go): confirm target repo → remove `C:\Users\Cyril\Projects\money-map-generator\.git\hooks\pre-push` → add remote → push.
5. Cleanup: `git worktree remove` for mm-lane-lease, mm-lane-slice10, mm-lane-slice6, mm-lane-specmod (junctions inside — remove via git, NEVER recursive-delete), prune merged lane branches (incl. lane/specmod).

## Known debt (log, don't do unless asked)
Unchanged from session 44: wizard hasWarnings hardcode; money-input locator rot; rapid-handoff race family; session40-app.test.ts brittle regex; tidy default-dims ceiling; vc regex false positives; codex-task exit-propagation bug; impeccable color-drift noise. Minor new (non-blocking, logged during diff audit): multitab:65's <5s timer wraps only focusPage, not the enabled-await; app-guidance Fit/aria-pressed assertion can pass vacuously if the Fit toggle is renamed; banner-takeover test could flake if fullForm outlasts lease staleness on slow runs — watch in full gate.

## Copy-paste prompt for fresh session

```text
Resume Money Map sprint close-out. Read C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40\docs\superpowers\handoffs\2026-08-03-session-45-handoff.md completely first — it has the full state. Branch repair/session-42 at cb5cf31, tree clean: lane/specmod merged + audited (5920923), slice-12 pan-hint gap fixed + fully gated (f8adfc7), all visual baselines regenerated + committed (cb5cf31, two audit findings in handoff §0 — one needs my dogfood verdict). VC policy HARD-ENFORCED — pushes/remotes blocked by design; the upload ceremony at the end goes through me explicitly.

Sequence: (1) final full-suite gate yourself (npx playwright test tests/e2e/ ~1.2h, 18 projects + npx vitest run 537 + npx tsc --noEmit) — a run may have completed last session, check for fresh results first; target 0 unexplained failures, expected-explained list in handoff §1 (chrome-layout:96 and visual:44/:62 must now PASS); (2) restart dev server on 4361, then I dogfood the routes in §2 incl. the two new checks; (3) upload ceremony with me; (4) lane-worktree cleanup (4 lanes, junctions inside — git worktree remove, never recursive-delete).

Standing rules: routing pyramid (Fable judgment-only; sonnet worktree lanes or luna via codex-task.ps1 for implementation — ONE codex at a time, no double quotes in -Prompt), hangup ratio, probe-first, delegate-digs, guardrails armed incl. vc, ONE playwright run at a time (port 4187), heavy reference skills only via sonnet subagent. Never trust worker self-reports over diffs; rerun verification yourself before committing.
```
