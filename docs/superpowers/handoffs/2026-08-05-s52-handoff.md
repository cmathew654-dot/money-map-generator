# S52 Handoff — O-ROT2 + P4-1/P4-2 COMPLETE, P4-3..5 remain (2026-08-05 ~23:00)

**Read fully. Canonical state.** Working dir: `C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40` (branch `repair/session-42`). Supersedes the P4 section of `2026-08-04-s51-sprint-handoff.md`; that doc's traps remain binding.

## State

- HEAD: `f05ea34`. **795/795 vitest, tsc clean, gate12 45/45 (0 FAIL, 0 PENDING — first fully-clean gate, independently confirmed).**
- s52 commits, all audited + verification rerun by Fable before commit:
  | sha | what |
  |-|-|
  | 50c2ddd | O-ROT2 click-again model (Cyril-DECIDED): first click on account text selects ACCOUNT; promotion only when selection already inside that account (sole/self/sibling drill-in); modifier-clicks always account. + Flow/Details text: mappings, canOpenDetails presence-gate, selectedMapAccountId derivation (row auto-expand), panelSelectionKeys echo fix. New tests/e2e/s52-click-again.spec.ts (7 contract items) |
  | 6131540 | docs: ledger item 1 DECIDED + REVERT-MAP row |
  | 47213cd | chip dblclick regression root-fix (editableLineTextProps pointerEvents selfInteractive-conditional) + gate12 B3/B7/B12 probes amended to shipped signatures |
  | 6fdf8fa | wizard row-focus echo through panelSelectionKeys (review F5, the second of two wirings) |
  | f05ea34 | aggregate notice persists staged font-size (review F6, 1-day-old T-RETYPE regression; applyMapTextEdit:596 refuses the value at the data layer) + commits docs/lanes/s52-adversarial-review.md |
- Adversarial review ADJUDICATED: 3 finder lenses → 13 findings → 6 refuters. **Zero data-integrity survivors. s52-caused: F5 (fixed) + F12 announcement gap (low, ledger).** Full verdict table: `docs/lanes/s52-adversarial-review.md` — THE input for the sign-off message ledger.
- Backup: full-history bundle (all refs incl. tonight) at `C:\Users\Cyril\Backups\money-map-s51\money-map-full-2026-08-05.bundle` — verified; Cyril copies it to a cloud drive. **Cloud push situation unresolved**: in-session push is denied by guardrails vc check (by design; `!` prefix routes through the session — the s51 sign-off's `!git push` line was self-defeating); Cyril reports an out-of-session failure too, error text never captured. First step next time: get the exact error (likely `!` pasted into PowerShell → '!git' not recognized, or GitHub credential missing — July publishes were token-via-API, no stored git credential guaranteed). Remote origin + read connectivity verified fine; remote has only main + repair/session-41.

## Remaining (P4-3..5, then P5)

1. **P4-3 audit + triage**: a full 19-project Playwright baseline run was in flight at handoff time (background; results incomplete — RE-RUN FRESH: `$env:PLAYWRIGHT_PORT='4290'; node_modules\.bin\playwright test --reporter=line`, expect 30-60+ min). Then triage every failure: known debt list in s51 handoff §P4-3 + NEW verdicts already made: `s51-retype.spec.ts:6` = stale selector (`details[data-account-id]` → T-FORM's `div.account-card`/`button.account-summary`, see Form.tsx:841-857) — fix the test; `map-keyboard :77/:110` pre-existing (:110 possibly real chip-drag bug). Then axe pass (map, Data panel, toolbar) + keyboard-only pass. **Visual re-bless: EYEBALL FIRST** — 5 auto-created pills-bench PNGs sit UNTRACKED in `tests/e2e/s51-pills-visual.spec.ts-snapshots/` (created blind by a lane run — never commit unseen); full-page visual.spec.ts baselines stale by design, re-bless after eyeballing.
2. **P4-4 deploy**: `npm run build:demo` → kill 4280 listener (`-State Listen` only, PID 0 TIME_WAIT is noise) → `node_modules\.bin\vite preview --outDir demo-dist --host 0.0.0.0 --port 4280 --strictPort` → hash-verify served index-*.js vs demo-dist.
3. **P4-5 gallery + sign-off**: afters matching `C:\Users\Cyril\Backups\money-map-s51\gallery\before\` (8 PNGs); update artifact 09c64dd3-1f59-40f5-927d-e4220fa4f311 or new; ONE message to Cyril = sign-off ledger (s51 handoff items 2-4 + the s52-adversarial-review.md ledger + O-ROT2 sibling-drill-in ruling) + retest list + push line; tag `s51-m4` + zip to `C:\Users\Cyril\Backups\money-map-s51\`.
4. **After Cyril + dogfooder pass**: push ceremony (s46/s47 mechanics; vc guard is Cyril's to lift), prune ~25 worktrees + merged lane branches.
5. **P5 (Cyril-mandated, Fable drafts at full effort)**: systematic audit-program plan. Requirements: WebSearch at draft time for AI-generated-code QA best practice (adversarial fleets, mutation/property/metamorphic testing); subsystem rotation persistence/undo → Form/Data → layout → print/export; a11y as own compliance workstream; replace free-click dogfooding with session-based charters + automated monkey-testing. Tonight's machinery (3 lenses → dedup → 2 refuters/finding, blind RED-first test lanes) is the template — it turned 7 filed HIGHs into 0 surviving HIGHs and 2 real fixes.

## Decisions LOCKED (do not re-ask)

- Click-again model incl. **sibling drill-in** (label→value promotes directly). Ledger item 1 DECIDED (sha 50c2ddd).
- gate12 B3 asserts the designed chip no-op (chip never joins multi-selection, per O-SEL + tests/s51-selection.test.tsx).
- Review findings triage: pre-existing → ledger (Cyril's fix-now-or-s53 call at sign-off); only s52/s51-sprint-caused got fixed in-session.

## Traps (new this session — s51 handoff traps ALL still binding)

- **`git worktree remove` follows node_modules JUNCTIONS on Windows and deletes through them into the shared install.** Unlink first: `cmd /c rmdir <lane>\node_modules`, THEN worktree remove. Restore if hit: `npm install` in s40 (never pnpm). Cost: one real incident this session (restored + gate re-certified).
- gate12 runs on port **4298** per its own header (the s51 handoff's "4281" is stale). Build `--outDir gate-dist`, preview `--host 127.0.0.1`.
- Playwright `.dblclick()` on chip glyphs is refused by actionability (hit rect owns the point) — raw `page.mouse.dblclick` is the user path and house pattern (gate12 B4).
- Everything renders twice (hidden print copy) — `.first()` on ANY map locator, including `[data-edit-line-node]`.
- Ports used tonight: 4289-4293 (lanes/e2e), 4298 (gate). 4280 still serves the OLD s50 build until P4-4.
- Fresh session must reload nothing else: this doc + `docs/lanes/s52-adversarial-review.md` + s51 handoff = full context.

## S53 prompt (copy-paste)

> Read C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40\docs\superpowers\handoffs\2026-08-05-s52-handoff.md FULLY first — canonical, decisions LOCKED; then docs\lanes\s52-adversarial-review.md (review ledger) and the s51 handoff's traps. cd C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40. HEAD f05ea34: O-ROT2 + P4-1/P4-2 done (795/795, gate12 45/45 clean). Your job: P4-3..5 per the s52 handoff — (1) fresh full 19-project Playwright baseline + triage (verdicts already made are listed; eyeball-then-bless visuals incl. 5 untracked pills PNGs); (2) axe + keyboard passes; (3) deploy 4280 + hash-verify; (4) gallery afters + ONE sign-off message (ledger from s52-adversarial-review.md + s51 items 2-4) + tag s51-m4 + zip. Then P5: Fable drafts the research-backed audit-program plan (requirements in handoff §5). Orchestration: Fable audits/judges; Opus agent lanes; codex benched. Binding: npm not pnpm; node_modules\.bin direct; PLAYWRIGHT_PORT pattern; data-account-id + .first(); UNLINK node_modules junctions before any worktree remove.
