# S54 Handoff — Batch A quick fixes + Batch B selection reducer (2026-08-05 ~02:30)

**Read fully. Canonical.** Working dir `C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40` (branch `repair/session-42`). HEAD `54f95c9`. All s53/s54a receipts green at HEAD: 795/795 vitest, tsc, gate12 45/45, stability green, 4280 serving `index-BAlG4Z4C.js` hash-verified. s51-m0..m4 + backup/s51 pushed to GitHub. Ceremony cut is LAW (Cyril 2026-08-05): half-page handoffs, 2-3 commits per batch, single verify pass, no triage essays. P5 audit program is DEAD. Accessibility batch PARKED unless recruiter-visible.

## Open rulings (ask Cyril first, one word approves all)
1. Mid-edit money format: default = formatted ($2,400), spec-expectation edit only.
2. Safari dblclick: default = "Best in Chrome or Edge" line on public demo now, webkit fix s56.
3. RESET ITEM clip (s46): default = close as moot (inspector redesigned twice since).

## Dogfood-first override
If Myranda's results file/text exists at session start (s54-dogfood-results.txt or pasted), triage it BEFORE Batch A — her breaks outrank everything below. Route: docs/dogfood/s54-dogfood-route.html.

## Batch A — independent quick fixes (one Opus lane, contract-frozen, ~90 min pattern proven in s54a)
1. Arrow-editor unconditional onFocus clobber — src/render/MapSvg.tsx ~:1872; copy the shouldFocusSelect guard shipped tonight at :3175 (sibling pattern :3377, helper :3570). New assertion.
2. Footnote edit-hit rect intercepts note shift-click — existing RED context tests/e2e/s51-selection-context.spec.ts:116 (pre-existing since 2026-07-28). Fix hit-rect layering, spec goes green.
3. Ellipse-cap intercepts canvas clicks — existing RED tests/e2e/canvas-editor.spec.ts:373. Fix intercept, spec goes green.
4. Editor rail overflows short viewports (Help off bottom at 360px tall) — .editor-rail/.workspace src/styles/app.css:557-586 (buttons flex:1). Fix = let rail compress/scroll at short heights. Extend chrome-layout.spec.ts.
5. Dead coverage-note focus path — note needs ~283px at 9px floor vs 217px room on 257px card (evidence in s54a lane report). STOP-and-report if the render-vs-remove call feels ambiguous; Fable decides.
Per-fix: RED first, isolated diff, STOP on premise-mismatch (s54a's lease row proved the premise CAN be wrong — spec was already green).

## Batch B — selection reducer (the spine; do NOT patch symptoms individually)
Single owner for selection state. Today: MapSvg decides (nextSelectedTargetKeys :3525, toggleSelectedTarget :2320, accountTextClickKey :3552) while 19 App.tsx setter call-sites + 9 onFocus handlers + panelSelectionKeys (App.tsx:173-179, modifier-blind) also write in the same tick. Fix-chain history b2324d0→1d1c504→50c2ddd→6fdf8fa is the proof of the multi-writer defect.
Sequence: (1) Opus design lane proposes the reducer (event alphabet, state shape, migration map of all writer sites) — Fable gates the design; (2) impl lane(s) migrate, focus handlers dispatch intent only; (3) ONE property test over event permutations (not a suite); (4) verify these die with it: wrong-handle multi-select, modifier-deselect row collapse, sidebar Shift-blindness, F9 focus steal; O-ROT2 click-again contract (s52-click-again.spec.ts, 7 items) must stay green — it is Cyril-decided behavior.
Worktree-per-lane only if lanes run builds concurrently (playwright webServer shares dist/).

## Hierarchy + token rules (as practiced, binding)
Fable = judgment only: contracts, diff skims, eyeball gates, commits, rulings. Opus general-purpose lanes implement (codex BENCHED). Explore/Haiku for digs; visual-reviewer for screenshots. Never trust lane self-reports — skim the diff yourself. Over ~50k context: delegate self-contained tasks. Heavy reference skills never load in main context. Stall-heartbeat = Monitor on artifact mtimes, never task .output files (they stay empty until final response); stop monitors when lanes report.

## STANDARD BLOCK (verbatim in every lane prompt)
npm-locked — pnpm FORBIDDEN in all forms including `pnpm exec`; no npx; no npm install; `node_modules\.bin` direct; no servers unless the contract names the port; no `--update-snapshots`; no commits; never remove worktrees; `[data-account-id]` is ambiguous (panel sidebar + hidden print copy) — use `[data-account-id="<id>"][role="group"]` + `.first()`; `PLAYWRIGHT_PORT=<port>` + `--output=<unique>` per run; driver lanes need SELECTOR-MAP + TIMEOUT-BUDGET (3-5s) tokens, non-driver lanes the NO-DRIVER-WORK token (lane-policy hook enforces).

## Traps (each cost real time once)
PS5.1 mojibake: file content NEVER through Get-Content/Set-Content/Out-File — git-native transfer (temp commit in lane worktree + `git checkout <sha> -- <files>`) or Edit/Write tools. Multi-stage shell = .ps1 file. commit-msg hook demands conventional format (temp commits too). Behavior specs gate on project `chromium-1280x720` (repo idiom — copy the beforeEach skip from s54-quickwins.spec.ts). vite preview CACHES at startup — rebuild then restart 4280 (taskkill the chain, Stop-Process is classifier-blocked; hash-verify via Invoke-WebRequest -OutFile). gate12 recert (script pattern: build gate-dist, preview 127.0.0.1:4298, node gate12.mjs, expect 45/45) required if CSS changes; visual churn → eyeball diffs, bless, stability re-run without update flags. UNLINK junctions (`cmd /c rmdir <wt>\node_modules` via PowerShell tool) before any worktree remove. Keep mm-s53-* worktrees. ONE writer per repo. Push is Cyril's ceremony: rename pre-push hook → push → rename back (auth is fine, GCM).

## Close-out
2-3 commits total, ledger delta appended to docs/lanes/s52-adversarial-review.md (table rows only), 4280 redeployed + hash-verified, memory updated, handoff refreshed half-page.

## S54 prompt (copy-paste)

> Read C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40\docs\superpowers\handoffs\2026-08-05-s54-handoff.md FULLY first — canonical; ceremony cut is law (half-page handoffs, 2-3 commits, single verify pass, no essays); P5 dead; a11y parked. cd C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40 (HEAD 54f95c9). FIRST: ask my three one-word rulings (money format / Safari line / RESET ITEM — defaults in handoff). SECOND: if Myranda's dogfood results exist, triage them before anything — her breaks outrank the batches. THEN Batch A: one Opus lane, contract-frozen per handoff (5 fixes, pinned sites, RED-first, STOP on premise-mismatch), you skim the diff, single verify pass (vitest+tsc+named specs+gate12 if CSS), eyeball any baseline churn, commit, redeploy 4280 hash-verified. THEN Batch B: selection reducer per handoff sequence — design lane → your gate → impl lanes → one property test → verify the four symptoms die and s52-click-again stays green (Cyril-decided contract). Hierarchy: Fable judgment-only, Opus lanes implement, codex benched, Explore/Haiku digs, STANDARD BLOCK verbatim in every lane prompt, never trust self-reports, stall-heartbeat via artifact mtimes not task files. Traps are in the handoff — mojibake (git-native transfer only), chromium-1280x720 spec gating, vite preview cache, junction unlink, ONE writer. Push stays my ceremony.
