# Session 42 Task 7 Handoff

This is the copy-paste handoff for a fresh window. Continue the existing
worktree; do not restart Tasks 1–6.

## Resume point

- Worktree: `C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40`
- Branch: `repair/session-42`
- Implementation baseline: `916ba1b docs: hand off session 42 task 7`
- Previous implementation HEAD: `9c9f86a feat: streamline clients and flows`
- Remote: nothing pushed; never push or add a remote
- Next task: Task 7, `Integrate, verify, and report`
- Stop after the Task 7 report and one local commit

## Read completely, in order

1. `C:\Users\Cyril\AGENTS.md`
2. `C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40\AGENTS.md`
3. `C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40\docs\superpowers\specs\2026-08-02-canvas-first-editor-design.md`
4. `C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40\docs\superpowers\plans\2026-08-02-canvas-first-editor.md`
5. This handoff: `docs/superpowers/handoffs/2026-08-02-session-42-task-7.md`
6. `C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40\.superpowers\sdd\2026-08-02-canvas-first-editor\progress.md`

At the start, invoke the same process skills: `superpowers:using-superpowers`,
`superpowers:executing-plans`, `superpowers:test-driven-development`,
`superpowers:requesting-code-review`, and
`superpowers:verification-before-completion`. Ponytail full remains active.

## What is already complete

### Task 3 — Data and selection synchronization

- Existing `Form` is still the canonical editor; App remains the only book,
  client, history, persistence, and writer owner.
- Data filtering/section focus, Details-to-Data focus, canvas selection
  synchronization, immediate money edits, null-preserving blanks, and stale
  focused-draft invalidation are implemented.
- Clear, Reset, undo, client changes, drag, autosave, and reload cannot revive
  an obsolete focused draft.

### Task 4 — Add, Contents, and Help paths

- Rail panels use callback-only App ownership, focus heading on open, restore
  focus on close, and restore hidden-flow state safely.
- Add, Contents, Help, empty-map creation paths, and keyboard/accessibility
  behavior are implemented without a second editor or dependency.

### Task 5 — Multi-selection commands

- `selectedMapTargetKeys` is App-owned; the last selected compatible account or
  note is primary.
- Align/distribute, internal copy/paste, duplicate, and delete reuse existing
  layout overrides, history, and duplicate helpers. Each model command is one
  undoable step; Income and Need remain semantic singletons.
- Modifier selection is safe around editable text, and shortcut handling yields
  to inputs/selects/text editors.
- Commit: `19e6533 feat: align selected map items`.

### Task 6 — Client combobox and connector flows

- `ClientCombobox` searches normalized client title/year, exposes native
  combobox/listbox semantics, supports Arrow navigation, Enter, Escape, and
  focus restoration, and routes selection through App.
- The primary Income, Need, or account exposes one pointer-only connector
  handle. Valid drops reuse `addCustomArrow`; self, duplicate, invalid, empty,
  Escape, and pointer-cancel paths do not create history.
- Print/export MapSvg has no connector chrome.
- Commit: `9c9f86a feat: streamline clients and flows`.
- Full Task 6 evidence: `npm test` — 28 files / 514 tests passed; `npm run
  build` — 61 modules transformed, exit 0; sequential focused Chromium — 15/15
  passed on `chromium-1280x720`.

## Task 7 scope

Approved files from the plan:

- Modify: `src/App.tsx`
- Modify: `src/styles/app.css`
- Modify: `tests/e2e/visual.spec.ts`
- Modify only affected visual snapshot files
- Create: `docs/codex/SESSION-42-REPORT.md` (required report exception)

Do not edit unrelated files or refactor oversized owners. If an affected e2e
test outside the map must be adapted because it still assumes the removed
native Active-client `<select>`, keep the smallest possible change and record a
one-line file-map justification in the report. Known files are:
`tests/e2e/interaction-regression.spec.ts`,
`tests/e2e/extended-certification.spec.ts`, and
`tests/e2e/reflow.spec.ts`.

### Required behavior

1. Tidy uses `tidyArrangement`, is disabled when already generated, commits one
   history step, and reports `Map layout reset`.
2. Add, duplicate, flow, reset, align, distribute, and delete commands provide
   short plain-language feedback.
3. At 1180px and 200% zoom, the open panel overlays the canvas and scrolls
   independently; the map is not pushed below the full Data form.
4. Present mode, print, PNG, PDF, and SVG contain no rail, panel, inspector,
   toolbar, selection visuals, or editing hit areas.
5. Preserve saved books, layout/text overrides, null money blanks, undo/redo,
   writer ownership, and App as the sole state owner.

## Orchestration contract

- Sol high is the orchestrator, gate runner, reviewer, and commit/stop
  authority. Sol does not silently accept worker-reported green results;
  rerun or inspect every required gate.
- Use exactly one direct-CLI Luna Max Priority implementation stream for Task 7.
  A second agent may scout read-only, but it must not edit shared product files.
  Never run concurrent implementation streams.
- Use strict TDD: add a smallest failing regression, run it RED, implement the
  minimum, run focused GREEN tests, then review the diff before broad gates.
- Reuse existing overrides/history/duplicate/layout helpers. No dependency,
  context provider, state library, second data snapshot, or speculative cleanup.
- Never run parallel Playwright. Use one worker and unique ports; begin with the
  configured `chromium-1280x720` project and run WebKit sequentially afterward.
- Every browser or app-control command has a hard timeout. If a Windows process
  wrapper hangs, stop it promptly, try the documented direct-patch or CLI
  fallback once, and report the runner deviation rather than waiting forever.

## Existing-file patch fallback

If normal patch mediation returns `windows sandbox: CreateProcessWithLogonW
failed: 2`, use Codex direct patch mode. Do not rewrite source with Node,
Python, or PowerShell:

```powershell
$patch = '<complete apply_patch payload>'
$patchArg = $patch.Replace([char]34, '\' + [char]34)
& 'C:\Users\Cyril\AppData\Local\Programs\OpenAI\Codex\bin\codex.exe' '--codex-run-as-apply-patch' $patchArg
```

## Verification sequence

Run each browser command serially with `--workers=1`; do not update snapshots
until the failing diff is understood.

```powershell
npx vitest run tests/mapedit.test.ts tests/map-inspector.test.tsx tests/session40-app.test.ts
$env:PLAYWRIGHT_PORT='4320'; npx playwright test tests/e2e/canvas-editor.spec.ts tests/e2e/interaction-regression.spec.ts --project=chromium-1280x720 --workers=1 --reporter=line
$env:PLAYWRIGHT_PORT='4321'; npx playwright test tests/e2e/canvas-editor.spec.ts tests/e2e/interaction-regression.spec.ts --project=webkit --workers=1 --reporter=line
npm test
npm run build
npm run build:demo
npm run test:visual
git diff --check
git status --short
```

The plan's broad regression files may expose old `selectOption` assumptions;
adapt them to the combobox only when the Task 7 file-map rule permits it, or
state the exact red/deviation in the final report. Quote actual command output;
never fabricate a green gate.

## Actual Chrome/Windows dogfood

Announce before controlling the user's visible Chrome/WinApp and use a hard
timeout. Exercise: open Data; type Salary/Wages `5000` and see the card update;
drag Income Sources with Data open; Tidy; Reset; Clear/confirm; blur/drag/reload;
multi-align; connector create/cancel; client title/year search; 200% zoom;
Present; print/export. Finalize browser control immediately. If the runner is
unavailable or returns sandbox error 1344, report setup once and stop retrying;
do not claim actual-browser certification.

## Report and stop boundary

Create `docs/codex/SESSION-42-REPORT.md` with file-by-file LOC, the verbatim
`npm test` and `npm run build` outputs, focused Chromium/WebKit evidence,
actual-Chrome evidence (or the honest runner deviation), visual/export results,
file-map justifications, and intentionally deferred findings. This required
session report is the only planned write under `docs/codex`; do not edit other
codex or review docs.

Then make one local commit and stop:

```powershell
git add src tests docs/codex/SESSION-42-REPORT.md
git commit -m "test: certify canvas-first editor"
git diff origin/main...HEAD --check
git status --short --branch
git log --oneline origin/main..HEAD
```

Do not push. Final response must include the exact local SHA, actual gate
results, deviations, and the fact that the worktree is intentionally left
local.

## Copy-paste prompt for the new window

```text
Resume C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40 on repair/session-42 at the current branch tip (implementation baseline 916ba1b).

Read both AGENTS.md files, the canvas-first design spec and implementation plan, this handoff (docs/superpowers/handoffs/2026-08-02-session-42-task-7.md), and the SDD progress ledger completely before acting. Tasks 1–6 are complete, committed, reviewed, and checkpoint-green. Begin Task 7 only: integrate, visually/responsively certify, dogfood in actual Chrome/Windows when available, write docs/codex/SESSION-42-REPORT.md, commit locally, and stop. Never push or add a remote.

Use the same orchestrator framework: Sol high for orchestration, requirements, gates, review, and commit/stop decisions; exactly one direct-CLI Luna Max Priority implementation stream for coding; an optional second read-only scout only. Strict TDD. App.tsx is the sole owner. Reuse existing overrides/history/duplicate/layout helpers. No dependencies, no second data snapshot, no unrelated cleanup, and no concurrent implementation streams.

Task 7 must cover Tidy disabled/one-step feedback, plain-language Add/duplicate/flow/reset feedback, 1180px/200% overlay and independent panel scroll, and editor-chrome removal from Present/print/PNG/PDF/SVG. Use only the approved file map; if old e2e tests still use the removed native Active-client select, make the smallest justified adaptation or record the deviation honestly.

Use strict RED/GREEN tests, then sequential Playwright only (`--workers=1`, unique ports), starting with `chromium-1280x720` and then WebKit. Run full npm test, npm run build, npm run build:demo, npm run test:visual, git diff --check, and git status. Every command has a hard timeout; immediately use the documented Codex direct-patch fallback if the Windows wrapper hangs, and do not let a timeout stall the window.

Before visible Chrome/WinApp control, announce it. Test Data + Salary/Wages 5000, Data-open drag, Tidy, Reset, Clear/confirm, blur/drag/reload, multi-align, connector valid/cancel, client search, 200% zoom, Present, and export. If actual-browser setup is unavailable or returns sandbox error 1344, report that once and stop retrying; do not claim it passed.

Write the required report with file LOC, verbatim gate output, browser/visual/export evidence, deviations, and deferred work. Commit exactly the final local Task 7 change with `test: certify canvas-first editor`, verify status/log, report the exact SHA, and stop. Do not push.
```
