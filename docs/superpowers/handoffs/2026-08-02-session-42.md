# Session 42 Canvas-First Editor Handoff

## Resume point

- Worktree: `C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40`
- Branch: `repair/session-42`
- Base: `origin/main` at `e22b584`
- Latest implementation commit: `af06b14 fix: clarify map pointer interactions`
- Planning commits:
  - `672ffd9 docs: specify canvas-first editor`
  - `8388fda docs: plan canvas-first editor`
- Remote state: nothing from this branch has been pushed.

Read in this order:

1. `C:\Users\Cyril\AGENTS.md`
2. `docs/superpowers/specs/2026-08-02-canvas-first-editor-design.md`
3. `docs/superpowers/plans/2026-08-02-canvas-first-editor.md`
4. This handoff.

Do not restart the prior product audit, competitor research, production regression investigation, or design approval. The canvas-first direction is approved. Continue at Task 2 of the implementation plan.

## Completed clean slice: Task 1

`af06b14` implements and verifies:

- Removed the misleading single-object `Snap to alignment` inspector command. Drag-time snapping/guides remain.
- Editable text pointer-down no longer bubbles into card movement or creates implicit text-position overrides.
- Stored legacy text offsets still render and remain editable through explicit inspector/keyboard controls.
- Account text hover uses a text cursor and no longer applies the card-drag shadow.
- Account body hover retains the card shadow and grab cursor.
- Added `Reset all text positions…` to the existing Reset menu with confirmation, plain-language feedback, and one-step Undo.
- Resetting text positions removes only `dx/dy`. It preserves font size and all non-text/card layout overrides.
- Selected `Reset text position` uses the same offset-only helper.

Confirmed red-before-green evidence:

- Pointer/edit contract failed because `data-pointer-action` was absent.
- Inspector test failed because `Snap to alignment` remained.
- Hover browser test failed with the account drop shadow while text was hovered.
- Reset recovery browser test failed while the confirmation was a no-op.

Verification run after the final refactor:

- Focused Vitest: 3 files, 105 tests passed.
- Focused Playwright Chromium:
  - account spawn/selection/text/body drag independence passed;
  - text versus card-body hover affordance passed;
  - offset-only reset, font/card preservation, toast, and Undo passed.
- `npm test`: 28 files, 498 tests passed.
- `npm run build`: TypeScript and Vite production build passed; 58 modules transformed.
- `git diff --check` passed before commit.

No actual Chrome/WinApp control was used in this slice. Do not claim actual-browser dogfood yet.

## Next exact task

Start Task 2: replace the permanent left form with the canvas-first shell.

Use TDD:

1. Add the failing shell assertions in `tests/session40-app.test.ts` and a new `tests/e2e/canvas-editor.spec.ts`.
2. Run them and confirm failure because `form-pane` and Guide me/Full form still occupy the workspace and no rail exists.
3. Create only `src/ui/EditorRail.tsx`.
4. Add `editorPanel: 'add' | 'data' | 'contents' | 'help' | null` as interface state in `App`.
5. Render the existing complete `Form` only when Data is open. Do not create a second data editor or book snapshot.
6. Keep `Wizard` reachable for new-client/guided setup.
7. Add the 72px rail / 380px panel / below-1180px overlay CSS.
8. Run focused unit and Chromium checks; commit Task 2 separately.

The next session may continue Tasks 3-7 after Task 2 is green. Do not compress several red/green slices into an unverified rewrite.

## Constraints that matter

- App remains the sole state/history/persistence/writer owner.
- No new dependency, context provider, state framework, persistence model, or speculative graph-layout engine.
- Null money remains blank, never zero.
- Smart automatic layout is tabled. Current `Tidy map` remains deterministic generated-arrangement reset and must preserve financial content.
- Every financial field remains reachable through Data.
- Selection shortcuts must not run in inputs, selects, textareas, or the text editor.
- Do not push. `AGENTS.md` requires local commits only.
- Do not run ACL migration, recursive permission updates, or broad scans against `C:\Users\Cyril`.
- Do not take visible Chrome/Windows control without announcing it first. Keep final live dogfood brief and finalize control immediately.
- If browser/WinApp preflight returns sandbox error 1344, report runner setup once and stop retrying.

## Local tooling note

During this session the nested patch wrapper could create new files but sometimes failed to update existing files with:

`windows sandbox failed: CreateProcessWithLogonW failed: 2`

The repository itself and elevated shell were healthy. If the failure repeats, use Codex's own apply-patch executable through an exact argument array; do not switch to ad-hoc file rewrites. A fresh narrowly rooted session may not reproduce it.

## Closeout still required later

After Tasks 2-7:

- Chromium and WebKit focused regressions.
- Relevant visual baselines only if editor UI intentionally changes.
- Full `npm test` and `npm run build`.
- Actual Chrome/WinApp adversarial smoke, announced first.
- `docs/codex/SESSION-42-REPORT.md` with verbatim gates and LOC.
- Final local commits and clean worktree.
- No push unless the owner explicitly resolves the current `AGENTS.md` prohibition.
