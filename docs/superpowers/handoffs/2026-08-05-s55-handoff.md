# S55 Handoff — post Batch A/B + s55 bug/spec pass (2026-08-05 ~04:20)

**Read fully. Canonical.** Working dir `C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40` (branch `repair/session-42`). HEAD `dfff3be` (uncommitted: this handoff + ledger delta). Receipts green at HEAD: tsc, vitest 804/804, FULL e2e sweep 132/138 (6 known-red: 3 a11y-parked, 2 multitab-unclear, interaction-regression:852), s52-click-again 7/7, gate12 45/45 (at 62c9a09; CSS unchanged since), 4280 serving `index-Dil5awtj.js` hash-verified. Pushed through c6ba4a4; 950051e+dfff3be+docs NOT pushed. **Pre-push hook is DISARMED (`pre-push.disabled`)** — Cyril must re-arm: `Rename-Item C:\Users\Cyril\Projects\money-map-generator\.git\hooks\pre-push.disabled pre-push`.

## s55 additions (post 8c69a1d)
- `950051e` three root-cause fixes: undo-after-align atomicity (historyBaselineRef — gesture-start chip-freeze no longer becomes the undo target); income amount hit-rect amount-line-only; keyboard notes spawn at visible centre (DELIBERATE change, s49 unit expectation updated — Cyril ack pending).
- `dfff3be` five stale specs realigned (tests only): wizard entry flow, warning-free heading, ClientCombobox idiom, multitab money expectation formatted per ruling, banner copy.
- Money format Form focus path CLOSED AS-IS by Cyril (raw-on-focus stays).
- Full-sweep baseline: 15 failed -> 6 failed / 132 passed.

## What landed (s54)
- `62c9a09` Batch A: rail fits short viewports; "Best in Chrome or Edge" demo line; arrow shift-click survival spec. Fixes 1-3 premises disproven (specs green); fix 5 ruled KEEP (need card user-resizable).
- `e6b885b` + `8c69a1d` Batch B: `selectionReducer` in `src/render/selection.ts` is the ONE writer of selection state (`{keys, anchor}`). All four symptoms dead (wrong-handle, modifier-deselect collapse, sidebar Shift-blindness, F9 steal). MapSvg = DOM resolution only. Property test `tests/s54-selection-reducer.test.ts` (echo invariance + I1-I4, I4 reference identity — prune identity is load-bearing vs App prune-effect loop).
- RESET ITEM (s46) closed moot; R5 EditorPanels modifiers deferred; R6 blankCanvas reason cut.

## Cyril review queue (decisions made overnight, flag-not-block)
1. **R2 REVISED at gate**: `focus/reveal` is PRIMARY (keyboard-only via `shouldFocusSelect` gate) — an existing green spec pins Tab-to-select. Tab within multi-selection = no-op; `panel/rowFocus` echo stays subordinate.
2. **Money format OPEN**: Cyril ruled formatted mid-edit, but live behavior is RAW on focus (`Form.tsx:351` snapshot `String(value)`). Spec-edit-only scope couldn't cover it; needs a behavior call (also settles the s54a "multitab money format PARTIAL" row).
3. Pre-existing e2e debt: full sweep has 15 fails at HEAD AND at pre-s54 HEAD (identical set: pointer-interception + WCAG/reflow — a11y parked). Not s54 regressions; control-verified by revert.
4. `tests/e2e/reflow.spec.ts:55` hardcodes stale demo-banner markup — will drift (banner copy changed in 62c9a09).

## Backlog candidates
Safari webkit dblclick fix (line shipped, fix promised s56); target-size mode (video-compressor note — wrong repo, ignore); money-format behavior fix if ruled; reflow.spec banner de-hardcode.

## Traps (unchanged, binding)
PS5.1 mojibake (Edit/Write tools only); chromium-1280x720 spec gating; vite preview cache (rebuild then taskkill+restart 4280, hash-verify); UNLINK junctions before worktree removes; keep mm-s53-* worktrees; ONE writer per repo; STANDARD BLOCK verbatim in lane prompts; commit-msg hook wants conventional format; no `--update-snapshots`.
