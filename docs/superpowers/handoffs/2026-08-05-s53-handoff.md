# S53 Handoff — triage/fixes/bless/P5-draft COMPLETE, deploy/gate/gallery/sign-off remain (2026-08-05 ~01:30)

**Read fully. Canonical state.** Working dir: `C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40` (branch `repair/session-42`). Supersedes the P4-3 section of the s52 handoff; the 21f8985 inventory's open flags are ALL RESOLVED in `docs\lanes\s53-triage.md`. s51/s52 traps remain binding.

## State — HEAD `1e9391e`

| receipt | value |
|-|-|
| vitest | 795/795, tsc clean (battery stage 4/5, post-fix) |
| Fixed-test verify | interaction-regression :362/:742 green (were s51-caused dup-name strict violations) |
| Visual baselines | 18 re-blessed + 16 pills blessed, stability re-run green WITHOUT update flags |
| Full-matrix triage | COMPLETE + adjudicated — `docs\lanes\s53-triage.md` (12 clusters, provenance-tested, zero un-verdicted failures) |
| axe/keyboard | 0 new critical/serious; no traps; F10 DENIED; F14 NEW (keyboard multi-select unreachable); 52 SVG contrast-unverified |
| P5 plan | DRAFTED — `docs\lanes\s53-audit-program-plan.md` (10 workstreams, ⚖ decision points marked) |

s53 commits (each audited + battery-verified before commit): `c54a6f6` seam fix (form.css 126→124) · `fa7fcc5` test fixes (retype selector, ring hint seed, font-size scoping) · `c5be164` baseline bless (34 PNGs) · `1e9391e` docs (triage + P5 plan). Plus `21f8985` = prior session's inventory (resolved).

## Remaining runway (s53b — est. 45-60 min)

1. **Rebuild + redeploy 4280** (demo-dist is PRE-seam-fix): `npm run build:demo`. Kill the pnpm-owned 4280 chain — find PIDs fresh: `Get-NetTCPConnection -LocalPort 4280 -State Listen` then walk parents (was node 36012 ← cmd 7836 ← pnpm 37224; they may have changed). `taskkill /F /PID <n>` passes the classifier; `Stop-Process` does NOT. Also kill/restart the 4297 preview if lanes need it — it still serves the pre-seam bundle (vite preview CACHES at startup). Relaunch: `node_modules\.bin\vite.cmd preview --outDir demo-dist --host 0.0.0.0 --port 4280 --strictPort`. Hash-verify served index-*.js vs demo-dist (PS 5.1: Invoke-WebRequest **-OutFile** then Get-FileHash — `.Content` is a string, not bytes).
2. **gate12 recert** (CSS changed → recert required): build `--outDir gate-dist`, preview `--host 127.0.0.1 --port 4298 --strictPort`, `node gate12.mjs`. Expect 45/45.
3. **Gallery**: re-shoot ONLY `data-panel.png` + `data-panel-scrolled.png` afters on the post-seam build (rest of `C:\Users\Cyril\Backups\money-map-s51\gallery\after\` is current, 8/8 captured + inspected). Update artifact 09c64dd3-1f59-40f5-927d-e4220fa4f311 (or new) with before/after pairs.
4. **ONE sign-off message to Cyril**, ingredients all on disk: s52-adversarial-review.md ledger + s51 handoff items 2-4 + O-ROT2 ruling (s52 handoff) + s53-triage.md headlines + ⚖ decision list (below) + dogfooder retest list + push line (s51 handoff bottom).
5. **Tag `s51-m4` + zip** to `C:\Users\Cyril\Backups\money-map-s51\`. Then memory update.
6. After Cyril + dogfooder pass: push ceremony (vc guard is Cyril's to lift), prune worktrees (protocol below).

## ⚖ Cyril decisions pending (present in sign-off)
Ledger fix-now-or-later: universal trio (multitab money-format, 28px inspector buttons, lease-banner timeout) · footnote hit-rect (s56?) · ellipse-cap intercept (s56?) · bench-covers-Help reflow bug. Plus: webkit dblclick (fix / defer / Chrome-Edge-only) · F14 keyboard multi-select model · mutation threshold · 2.5.7 drag-alternatives scope · target-size bar 24 vs 32px · monkey-run host. And: OneDrive sign-in (bundle at `C:\Users\Cyril\OneDrive\Backups\money-map\` is LOCAL-ONLY until signed in) · capture exact push error text next attempt · close any other Claude window on this repo (two-session overlap happened tonight — see triage doc Ops notes).

## Orchestration doctrine (as practiced + refined tonight)

- **Tiers:** Fable = judgment only (triage verdicts, eyeball gates, diff audits, commits, sign-off). Implementation/investigation = parallel **Opus agent lanes** (general-purpose; visual-reviewer for screenshot work — it found the seam). codex BENCHED. Explore/Haiku for pure evidence digs. Fable inline edits only under de-minimis (one-selector fixes mid-gate).
- **Worktree rule (the real reason):** worktree-per-lane is required when lanes run BUILDS from the tree — playwright's webServer runs `npm run build` into the shared `dist/`, so two concurrent runs from one tree corrupt each other. Read-only lanes (axe, gallery, research) need NO worktree. Pattern: `git worktree add <path> <sha>` + junction `cmd /c mklink /J <wt>\node_modules <s40>\node_modules` (run via the **PowerShell tool** — `cmd /c` silently no-ops under the Bash tool, returns an interactive banner).
- **Lane prompt STANDARD BLOCK, no exceptions** (tonight's pnpm-exec incident = this block was omitted from 2 of 5 prompts and a lane started a server with pnpm): npm-locked — pnpm FORBIDDEN in all forms including `pnpm exec`; no npx; no npm install; `node_modules\.bin` direct; no servers unless the contract names the port; no `--update-snapshots`; no commits; never remove worktrees; `[data-account-id]` + `.first()` (hidden print copy); `PLAYWRIGHT_PORT=<port>` + `--output=<unique>`; driver lanes need SELECTOR-MAP + TIMEOUT-BUDGET (3-5s) tokens, non-driver lanes the NO-DRIVER-WORK token (lane-policy hook enforces).
- **Hybrid baseline protocol:** 8-worker parallel sweep (20 min vs 1.9 h serial on this 20-core box, `--workers=8` CLI overrides the pinned config) then **serial re-confirm of every failure before any verdict** — fail-parallel-pass-serial = flake; fail-both = real. Provenance for non-ledger failures: re-run by TITLE (`-g`, line numbers drift) at the freeze tag in a worktree.
- **Never trust worker self-reports:** verify by process table / git diff / artifact hashes (tonight: pnpm chain caught via `Get-CimInstance` parent walk; fix lane's diff audited line-by-line; its STOP condition on gate12-referenced rename was correct and honored).

## Hangup / monitoring practices

- **Stall heartbeat (Monitor tool):** poll newest artifact **mtime** every 60s across the lanes' write targets; emit only on >120s silence or completion. DO NOT key on agent task `.output` files — they stay empty until the final response, so completion detection must come from task notifications; STOP the monitor when lanes report or it becomes the stale noise itself.
- **lanewatch stale flags:** the hook keys on output-file growth and never learns completion → it re-flags finished lanes on every tool call. Dismiss against evidence already in hand (zero probes). Real fix = guardrails.js patch (classifier blocks agent edits — Cyril's patch; diagnosis in P5 plan §J).
- **Shell traps that cost time tonight:** nested quotes inside `cmd /c "…"` shred at `|` (a `-g "a|b"` literally executed Windows `RESET`) → multi-stage runs go in a **.ps1 script file** with per-stage `$LASTEXITCODE` checks; PS 5.1 `Invoke-WebRequest .Content` is a string (use `-OutFile` for hashing); `Start-Sleep` chains are blocked (use Monitor until-loops); vite preview binds IPv6-only without `--host`.
- **Ports registry:** 4280 demo (pnpm-owned chain to replace) · 4290 ORPHAN (PID 15816, prior session's webServer — kill or ignore) · 4297 my preview (pre-seam bundle — stale) · 4292-4296 used by tonight's runs (free) · 4298 gate12 · 4187 config default (never hit).

## Standing worktrees (KEEP until s54 — traces live inside)
`mm-s53-prov` @ s51-m0 (provenance receipts, test-results-prov*) · `mm-s53-zoom` @ d6f741f (zoom traces) · `mm-s53-scatter` @ 6131540 (moved for the webkit probe; scatter traces). All have junctioned node_modules: **UNLINK FIRST** (`cmd /c rmdir <wt>\node_modules` via PowerShell tool) before any `git worktree remove`. Untracked evidence dirs in s40 (`test-results-*`, `gate-dist`, `demo-gate`, `repro-lpin.mjs`) — leave until after m4.

## S53b prompt (copy-paste)

> Read C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40\docs\superpowers\handoffs\2026-08-05-s53-handoff.md FULLY first — canonical, all triage verdicts final in docs\lanes\s53-triage.md, decisions LOCKED, do not re-triage. cd C:\Users\Cyril\Projects\.worktrees\money-map-generator-s40. HEAD 1e9391e: triage + fixes + baseline bless + P5 plan DONE (795/795, tsc clean, battery green). Your job = the handoff's Remaining runway: (1) npm run build:demo, replace the 4280 server (taskkill the pnpm chain — fresh PIDs via Get-NetTCPConnection — then vite preview 0.0.0.0:4280), hash-verify via -OutFile; (2) gate12 recert on 4298 (build gate-dist), expect 45/45, investigate any FAIL before proceeding; (3) re-shoot data-panel + data-panel-scrolled afters on the new build (visual-reviewer lane, befores in C:\Users\Cyril\Backups\money-map-s51\gallery\before), update artifact 09c64dd3-1f59-40f5-927d-e4220fa4f311; (4) ONE sign-off message per handoff §4 ingredient list incl. the ⚖ decision list; (5) tag s51-m4 + zip to C:\Users\Cyril\Backups\money-map-s51\; update memory. Orchestration per handoff doctrine: Fable judges/audits/commits, Opus lanes, codex benched, lane prompts carry the STANDARD BLOCK verbatim, stall-heartbeat via artifact mtimes not task files. Binding: npm not pnpm (pnpm exec included); node_modules\.bin direct; .ps1 files for multi-stage runs (no nested cmd quotes); PLAYWRIGHT_PORT + --output per run; data-account-id + .first(); UNLINK junctions (PowerShell tool) before any worktree remove; keep mm-s53-* worktrees until s54.
